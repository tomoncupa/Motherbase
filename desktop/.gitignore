# What the program writes while it runs, not what it is.
launcher.log
launcher.txt
status-app.log
status-app.txt
data/

# build.ps1 copies these three out of lib\ to sit beside the exe. The copies
# are build output; the originals in lib\ are the dependency and DO go in.
/Microsoft.Web.WebView2.Core.dll
/Microsoft.Web.WebView2.WinForms.dll
/WebView2Loader.dll
